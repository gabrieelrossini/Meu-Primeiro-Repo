/*Crie uma função verificaFrete que receberá um valor por parâmetro.
  Se esse valor for igual ou su- perior a 100, a função deve retornar 0.
 Se o valor for menor do que 100, deverá retornar 10% do valor informado.
*/

function verificaFrete(frete) {
  if (frete >= 100) {
    return 0;
  } else {
    return valor * 0.1;
  }
}